#include <iostream>

using namespace std;
int main(
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int val;
    int N;
    cin >> N;
    for (int i = 0; i < N; i++)
    {
        val = 1000 * i + 666;
        if (val / 10 % 1000)
    }
    1000 * 0 + 6666;

    
)